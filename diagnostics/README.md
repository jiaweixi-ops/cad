# Diagnostics

This directory contains host-side diagnostic evidence captured during development. These files are not plugin runtime data and are not used by the product.

- `accoreconsole-acad.err`: AutoCAD 2025 Core Console reported a delayed-load failure for `AcGeomentObj.dbx` before the plugin script ran.
